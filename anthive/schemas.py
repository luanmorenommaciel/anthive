"""anthive contract schemas.

STUB — populated by p0 per tasks/p0.md.

The four canonical contracts:
    1. TaskFrontmatter    — YAML frontmatter in tasks/*.md docs
    2. ReadyList          — output of `anthive scan`
    3. SessionLogFrontmatter — state of a running session
    4. MergeQueueRow      — row in logs/merge-queue.md

See tasks/p0.md for the full specification.
"""

# TODO(p0): implement Pydantic models per tasks/p0.md
